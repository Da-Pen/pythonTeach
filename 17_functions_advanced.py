# TODO. Cover:
#   - Default parameter values, optional parameters
#   - Specify parameter values by name
#   - *args
#   - functions as first-class objects (ex. can be assigned to a variable, can be passed as arg)
#   - functions that return functions (maybe cover as a part of first-class objects)
#   - lambdas (ex. sorted(l, key=lambda x: x[0]))
